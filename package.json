{
  "name": "collaborative-canvas",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon --watch server --watch client -e js,html,css server/server.js",
    "build": "tsc",
    "build:watch": "tsc --watch"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.8.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3"
  }
}
